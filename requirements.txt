# Jersey number pipeline — main environment
# Install: pip install -r requirements.txt
# Older README used torch 1.9.0; that build has no wheels for Python 3.11+.
# For GPU + CUDA on Linux, you can install PyTorch first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# then: pip install -r requirements.txt --no-deps
# (or omit --no-deps if you did not pin torch above)

# Core (2.x — works on modern Python; inference loads checkpoints with map_location)
torch>=2.2.0
torchvision>=0.17.0

# Vision & I/O
opencv-python
Pillow

# Data & numerics
numpy
pandas
scipy

# Utilities
tqdm
